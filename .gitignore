# Segredos -- nunca versionar (token da ConsultaJá/Indecx)
pipeline/.env

# Segredo -- nunca versionar (PAT do GitHub usado por atualizar_diario_n8n.py)
/.env

# Ambiente virtual Python (recriável via pipeline/requirements.txt)
pipeline/.venv/

# Log local do atualizador -- fica só na sua máquina
pipeline/atualizacoes.log

# Log local da rotina diária automática (n8n) -- fica só na máquina que roda
atualizacoes_diarias_n8n.log
__pycache__/

# Rascunho de script colado no chat -- não faz parte do pipeline
/codigo-consultaja.txt

# Configuração pessoal de editor
.vscode/

# dados-fonte/*.xlsx (dado bruto de paciente) já é ignorado pelo
# dados-fonte/.gitignore próprio -- não duplicar aqui.
# pipeline/__pycache__ e *.pyc já são ignorados por pipeline/.gitignore.

# Outros dashboards, cada um com seu próprio .git/remoto -- vivem juntos
# nesta pasta só por conveniência de workspace (ver CONTEXTO-GITHUB.md),
# nunca fazem parte deste repositório (Dashboard-de-NPS-Pacientes).
/agendas-pac-real/
/agendas_pgmed/
/csat/
/hub/

# Nota de workspace multi-repositório -- só referência local, não versionada
# (mesma convenção do CONTEXTO-GITHUB.md antigo, em troca-github/).
/CONTEXTO-GITHUB.md

# Nota de comparação com a conta antiga (TCM-18) -- rascunho de validação
# interna, não faz parte do dashboard nem deve ir pro repositório público.
/COMPARACAO_TCM18.txt
